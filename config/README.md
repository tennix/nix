# NixOS configurations for Tennix's mac book

configuration.nix and hardware-configuration.nix are global configurations in /etc/nixos

config.nix is normal user configuration file in $HOME/.config/nixpkgs
